// Proves the "logic" Jest project resolves and transforms TypeScript.
// Task 2 deletes this file and replaces it with real tests.
test('the logic test project runs', () => {
  expect(1 + 1).toBe(2);
});
