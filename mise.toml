# Toolchain for this repository. Run `mise install` after cloning.
[tools]
# The React Native app (repository root). Expo SDK 57 wants Node 20 or newer.
node = "24.16.0"
# The legacy Fyne app in legacy-fyne/.
go = "1.26.4"
# Android release builds.
"npm:eas-cli" = "latest"
